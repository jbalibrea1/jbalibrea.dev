---
const messages = ['Hey', 'Hola', 'Saludos', 'Qué tal', 'Hello', 'Hi'];
const randomMessage = () =>
  messages[Math.floor(Math.random() * messages.length)];
const msg = randomMessage();
---

<div>
  <p class="text-lg text-current sm:inline-block">
    <span aria-label="saludo">👋</span>
    {msg}! Me llamo{' '}
  </p>
  <h1
    class="font-extrabold text-3xl sm:text-4xl tracking-tight dark:text-white inline-block"
  >
    Jorge Balibrea
  </h1>
</div>
