---
const messages = ["Hey", "Hola", "Buenas", "Saludos", "Qué tal"];
const randomMessage = () =>
  messages[Math.floor(Math.random() * messages.length)];
const msg = randomMessage();
---

<p class="text-xl sm:text-2xl text-current flex flex-row gap-1 flex-wrap">
  👋 {msg}! Me llamo <span class="font-bold tracking-tight">Jorge Balibrea</span
  >
</p>
